# Sprint 1.1.2 — OTP Login & Invite Flow

## Contexto

Repositório: `aistudio-experience-connect-admin`
Stack: React 19 + Vite + TypeScript + Supabase + Edge Functions (Deno)
Monorepo: PNPM Workspaces + Turbo

## Estado Atual

- `AuthCard.tsx` em `apps/web/src/pages/auth/components/AuthCard.tsx` — login com email+senha (signInWithPassword). **Sem OTP.**
- `supabase/functions/` — 6 Edge Functions existentes (booking flow). Padrão: Deno + CORS headers + service role.
- `supabase/config.toml` — project_id = "aistudio-experience-connect-admin"
- Rotas existentes em `router/config.tsx`: `/login`, `/admin/*`, `/`
- `packages/core/src/` — tipos compartilhados

## Tarefas

### 1. Adicionar aba de login OTP no AuthCard

**Arquivo:** `apps/web/src/pages/auth/components/AuthCard.tsx`

Adicionar toggle entre "Email e Senha" e "Codigo Magico (OTP)" no formulario de login.

O fluxo OTP deve:
- Input apenas de email
- Botao "Enviar codigo magico" que chama `supabase.auth.signInWithOtp({ email })`
- Após envio, mostrar estado de confirmacao: "Codigo enviado para {email}. Verifique sua caixa de entrada."
- Input para digitar o token OTP recebido
- Botao "Confirmar codigo" que chama `supabase.auth.verifyOtp({ email, token, type: 'magiclink' })`
- Apos confirmacao, redirecionar para `/admin/dashboard`

Manter o login existente (email+senha) como opcao padrão. OTP como segunda aba/botao.

### 2. Criar Edge Function `invite-user`

**Arquivo:** `supabase/functions/invite-user/index.ts` (novo)

Seguir o padrao existente das Edge Functions (vide `create-booking-hold/index.ts`):

```typescript
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2.39.0';

const CORS_HEADERS = { ... }; // mesmo padrao

interface InviteUserRequest {
  email: string;
  tenant_id: string;
  role: 'admin' | 'operator' | 'guest';
}

// POST /invite-user
// 1. Validar se usuario logado tem role admin no tenant
// 2. Criar registro em invitations (tenant_id, email, role, token, expires_at)
// 3. Retornar link de invite: { invite_url: "/invite/{token}" }
// 4. (Futuro: enviar email)
```

Nao precisa enviar email real — apenas gerar o link e retornar.

### 3. Atualizar schema — criar tabela `invitations`

Adicionar migration `20260612000000_create_invitations.sql`:

```sql
create table public.invitations (
  id uuid primary key default gen_random_uuid(),
  tenant_id uuid not null references public.tenants(id),
  email text not null,
  role text not null check (role in ('admin', 'operator', 'guest')),
  token text not null unique,
  expires_at timestamptz not null default now() + interval '7 days',
  accepted_at timestamptz,
  created_at timestamptz not null default now(),
  created_by uuid not null references auth.users(id)
);
alter table public.invitations enable row level security;
-- Policy: apenas admins do tenant podem ver invitations do seu tenant
```

### 4. Criar pagina `/invite/:token`

**Arquivo:** `apps/web/src/pages/invite/page.tsx` (novo)
**Rota:** adicionar em `router/config.tsx`: `{ path: "/invite/:token", element: <InvitePage /> }`

A pagina deve:
- Ler o token da URL
- Buscar invitation: `supabase.from('invitations').select('*').eq('token', token).single()`
- Se valida (nao expirada, nao aceita): mostrar formulario de aceite
- Usuario faz login (ou cria conta) via OTP
- Apos login: `supabase.rpc('accept_invite', { invitation_token: token })`
- A RPC `accept_invite` deve: validar token, criar registro em `user_tenants`, marcar `accepted_at`, deletar invitation
- Redirecionar para `/admin/dashboard`

### 5. Criar RPC `accept_invite`

Adicionar na migration:

```sql
create or replace function public.accept_invite(invitation_token text)
returns void
language plpgsql
security definer
as $$
declare
  inv_record record;
begin
  select * into inv_record from public.invitations
  where token = invitation_token
    and accepted_at is null
    and expires_at > now()
  for update;

  if not found then
    raise exception 'INVITATION_INVALID_OR_EXPIRED';
  end if;

  insert into public.user_tenants (user_id, tenant_id, role)
  values (auth.uid(), inv_record.tenant_id, inv_record.role);

  update public.invitations
  set accepted_at = now()
  where token = invitation_token;
end;
$$;
```

## Padrões

- Edge Functions: Deno + CORS headers + service role (seguir `create-booking-hold/index.ts`)
- Migrations: arquivos em `supabase/migrations/20260612000000_create_invitations.sql`
- RPC: `security definer` para operações que cruzam tenant boundary
- Auth: `supabase.auth.signInWithOtp()` + `supabase.auth.verifyOtp()`
- UI: manter padrao visual existente (Tailwind + shadcn/ui)

## Gates (executar após implementação)

```bash
pnpm typecheck  # zero erros
pnpm lint       # zero erros
pnpm build      # sucesso
```

## Critérios de Aceite

- [ ] Aba OTP funcional no AuthCard (email → codigo → confirmacao → redirect)
- [ ] Login existente (email+senha) mantido como padrao
- [ ] Edge Function `invite-user` criada
- [ ] Migration `20260612000000_create_invitations.sql` com tabela + RLS
- [ ] RPC `accept_invite` implementada
- [ ] Pagina `/invite/:token` com fluxo de aceite
- [ ] `pnpm typecheck` — 0 errors
- [ ] `pnpm lint` — 0 errors
- [ ] `pnpm build` — ok

## Arquivos a modificar/criar

1. `apps/web/src/pages/auth/components/AuthCard.tsx` — adicionar aba OTP
2. `supabase/functions/invite-user/index.ts` — nova Edge Function
3. `supabase/migrations/20260612000000_create_invitations.sql` — nova migration
4. `apps/web/src/pages/invite/page.tsx` — nova pagina
5. `apps/web/src/router/config.tsx` — adicionar rota /invite/:token

## Nao alterar

- Logica de negocio existente (booking, pagamento, etc.)
- Schemas existentes (tenants, user_tenants, bookings, etc.)
- UI components fora do escopo
