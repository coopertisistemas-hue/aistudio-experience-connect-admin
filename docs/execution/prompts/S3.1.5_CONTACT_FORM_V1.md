# Sprint 3.1.5 — Contact Form

**Exec Agent:** Kimi  
**Date:** 2026-06-12  
**Milestone:** Wave 3 — Public Site

---

## Context

Landing page has a "Fale Conosco" CTA but no actual contact form. The Footer has contact info placeholders.

---

## Scope

### 1. Contact Page
Create `apps/landing/src/pages/Contact.tsx` (rota `/contato`):
- Name, email, phone, subject, message fields
- Form validation (required fields, email format)
- Submit → `invokeEdgeFunction('send-contact-email')` or store in Supabase
- Loading state on submit
- Success state with confirmation message
- Error state with retry
- Dark theme matching existing pages

### 2. Contact Service
Add to `apps/landing/src/services/booking.ts` or create `contact.ts`:
- `submitContact(data)` — stores contact in a `contact_messages` table via edge function or direct insert

### 3. Migration
Create `supabase/migrations/20260612100700_create_contact_messages.sql`:
- Table: `contact_messages (id, tenant_id, name, email, phone, subject, message, created_at)`
- RLS: insert for anon, select for admin only
- Idempotent

### 4. Update Header + Footer
- "Contato" nav link → `/contato`
- Footer contact section → consistent styling

---

## QA Gates
1. `cd apps/landing && pnpm typecheck && pnpm lint && pnpm build`
2. Root `pnpm typecheck`
3. Codex audit → fixes → Agy

---

## Delivery
Report: files created/modified, verification results, per-item status.